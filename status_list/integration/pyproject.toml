[tool.poetry]
name = "status-list-integration"
version = "0.1.0"
description = ""
authors = []
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"
pytest = "^8.3.5"
pytest-asyncio = "^1.2.0"
requests = "^2.33.0"

[tool.poetry.group.dev.dependencies]
black = "^26.3.1"
ruff = "^0.15.4"

[tool.pytest.ini_options]

[build-system]
requires = ["poetry-core>=2.1"]
build-backend = "poetry.core.masonry.api"

